lets optimize for utilities sector

1. Adjust Revenue Scoring
Utilities tend to have stable but lower revenue growth.

Suggestion: Reduce revenue weight slightly or normalize scores based on sector-specific averages

rev_score = min(max((revenue / 5e8) * 2, 0), 15)

2. Emphasize Debt Ratios
Utilities often carry more debt due to infrastructure investments.

Suggestion: Be less punitive for higher debt-to-equity ratios in this sector.
# Allow higher D/E by changing the penalty curve
debt_score = max(0, 20 - (debt_to_equity * 1.2))  # instead of *2

3. Focus More on ROE and Net Margin
Utilities are valued for stable returns and efficient operations.

Suggestion: Increase weight of ROE and net margin.

margin_score = min(max(net_margin * 2.0, 0), 25)
roe_score = min(roe * 0.6, 25)

dividend_yield = financials.get("dividend_yield", {}).get("value", None)
div_score = min(dividend_yield * 5, 10) if dividend_yield else 0